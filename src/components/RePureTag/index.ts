import pureTag from "./src/index.vue";
import { withInstall } from "@pureadmin/utils";

/** 默认无边框的标签，支持 Element Plus Tag 属性和事件。 */
export const PureTag = withInstall(pureTag);
export default PureTag;
